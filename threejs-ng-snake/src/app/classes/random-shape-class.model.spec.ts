import { RandomShapeClass } from './random-shape-class.model';

describe('RandomShapeClass', () => {
  it('should create an instance', () => {
    expect(new RandomShapeClass()).toBeTruthy();
  });
});
